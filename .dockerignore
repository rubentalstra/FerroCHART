# SPDX-FileCopyrightText: Ruben Talstra
# SPDX-License-Identifier: BUSL-1.1

# The build context of a container recipe is the repository root, so this file
# guards it: nothing enters the context but the staged binaries under dist/. A
# checkout carrying a target/ directory would otherwise send gigabytes to the
# builder.
*
!dist/
